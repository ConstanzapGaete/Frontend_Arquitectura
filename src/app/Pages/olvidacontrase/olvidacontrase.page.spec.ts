import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OlvidacontrasePage } from './olvidacontrase.page';

describe('OlvidacontrasePage', () => {
  let component: OlvidacontrasePage;
  let fixture: ComponentFixture<OlvidacontrasePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OlvidacontrasePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
